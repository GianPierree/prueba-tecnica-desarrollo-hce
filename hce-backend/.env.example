# ─── Base de datos SQL Server ────────────────────────────────
DB_HOST=localhost
DB_PORT=1433
DB_USERNAME=sa
DB_PASSWORD=Password123!_HCE
DB_DATABASE=master

# ─── Kafka ───────────────────────────────────────────────────
# En desarrollo local: localhost:9092
# En Docker Compose entre contenedores: kafka:9092
KAFKA_BROKER=localhost:9092

# ─── JWT ─────────────────────────────────────────────────────
JWT_SECRET=cambia_este_secreto_por_uno_seguro_en_produccion
JWT_EXPIRES_IN=30m

# ─── CORS ────────────────────────────────────────────────────
# URL del frontend Next.js (sin slash final)
FRONTEND_URL=http://localhost:3000
